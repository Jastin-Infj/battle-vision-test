apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    deployment.kubernetes.io/revision: "1"
  name: app
  namespace: default
spec:
  selector:
    matchLabels:
      name: app
  replicas: 1
  template:
    spec:
      containers:
      - name: react-app
        image: us-central1-docker.pkg.dev/app-battle-vision/app-battle-vision-ar/react-app:latest   # Edit
        ports:
        - containerPort: 3000
        imagePullPolicy: IfNotPresent
      imagePullSecrets:
        - name: gcr-secret
    metadata:
      labels:
        name: app
      namespace: default
---
apiVersion: v1
kind: Service
metadata:
  creationTimestamp: "2023-04-20T04:52:06Z"
  name: gcr-secret
  namespace: default
  uid: 69ad2b79-51c0-4440-869a-50085d4854ec
spec:
  type: NodePort
  selector:
    app: react-app
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: react-app